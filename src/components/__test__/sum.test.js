import {sum} from '../sum';

test('This is a test case to check if sum function is working properliy or not', () => {
  
    // const result = sum(3, 7);
    
    // // Assertion
    // expect(result).toBe(10);
})
